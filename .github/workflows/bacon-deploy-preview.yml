name: Preview DNS Records

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  preview:
    name: Preview ${{ matrix.domain }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    strategy:
      max-parallel: 1
      matrix:
        domain:
          - jungaretti.com
          - jamesungaretti.com
    steps:
      - uses: actions/checkout@v7
      - uses: jungaretti/bacon-deploy-action@v3
        with:
          api-key: ${{ secrets.PORKBUN_API_KEY }}
          secret-key: ${{ secrets.PORKBUN_SECRET_KEY }}
          config: dns/${{ matrix.domain }}.yml
          mode: dry-run
